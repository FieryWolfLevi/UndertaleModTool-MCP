<UserControl x:Class="UndertaleModTool.McpConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UndertaleModTool"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold" Margin="0,0,0,8">
            MCP Server Configuration
        </TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <Border Background="{Binding StatusText, Converter={x:Static local:McpStatusBrushConverter.Instance}}"
                    CornerRadius="3" Padding="8,2" Margin="0,0,12,0">
                <TextBlock Text="{Binding StatusText}" FontWeight="Bold" Foreground="White"/>
            </Border>
            <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Width="70" VerticalAlignment="Center"
                     IsEnabled="{Binding IsNotRunning}"/>
            <CheckBox Content="Start automatically on launch" IsChecked="{Binding AutoStart}" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <local:ButtonDark Content="Start server" Click="StartButton_Click" IsEnabled="{Binding IsNotRunning}" MinWidth="100" Margin="0,0,8,0"/>
            <local:ButtonDark Content="Stop server" Click="StopButton_Click" IsEnabled="{Binding IsRunning}" MinWidth="100" Margin="0,0,8,0"/>
            <local:ButtonDark Content="Open status page" Click="OpenStatus_Click" IsEnabled="{Binding IsRunning}" MinWidth="120"/>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Available tools" Margin="0,0,0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Tools}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:McpToolInfo}">
                            <TextBlock Text="{Binding Name}" Margin="2" FontFamily="Consolas"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="Log">
            <TextBox Text="{Binding Log, Mode=OneWay}" IsReadOnly="True" Height="120"
                     VerticalScrollBarVisibility="Auto" FontFamily="Consolas" TextWrapping="Wrap"/>
        </GroupBox>
    </Grid>
</UserControl>